import 'package:flutter/material.dart';

class CustomBiometricsPage extends StatelessWidget {
  const CustomBiometricsPage({super.key, this.customPaintSize = 200.0});

  final double customPaintSize;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}